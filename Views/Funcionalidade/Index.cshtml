@model IEnumerable<crudmysql.Models.Funcionalidade>


<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        <a asp-action="Create">Criar Novo</a>
    </p>
    @if (TempData["Message"] != null)
    {
        <div class="alert alert-success">@TempData["Message"]</div>
    }
    <table class="table">
        <thead>
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Nome)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Descricao)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Perfil)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Nome)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Descricao)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Perfil.Nome)
                    </td>
                    <td>
                        <a asp-action="Edit" asp-route-id="@item.IdFuncionalidade">Editar</a> |
                        <a asp-action="Details" asp-route-id="@item.IdFuncionalidade">Visualizar</a> |
                        <a asp-action="Delete" asp-route-id="@item.IdFuncionalidade">Excluir</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</body>
</html>
